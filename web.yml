- hosts: all
  tasks:
  - name: install package
    package:
      name: "httpd"
      state: present
  - name: Copy content
    copy:
      content: "welcome to do447 test web - add more content"
      dest: "/var/www/html/index.html"
  - name: Start service
    service:
      name: "httpd"
      state: started
